package pl.verdigo.libraries.drawer.internal;

import pl.verdigo.libraries.drawer.Drawer;
import android.content.Context;
import android.view.Window;

public class RightDrawer extends Drawer
{

	public RightDrawer(Context context, int layout, Window parentWindow, float drawerMargin, float landDrawerWidth)
	{
		super(context, layout, parentWindow, drawerMargin, landDrawerWidth);
	}

}
